spring:
  cloud:
    config:
      name: public-config,sleuth-config
      label: master
      profile: dev
      uri: http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/